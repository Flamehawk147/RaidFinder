-- namespace
local _, ns = ...;

local Dungeon = {
    -- the name of the dungeon
    name,
    -- dungeon or raid
    category,
    -- maximum number of players permitted within the instance
    maxPlayers,
    -- TODO maybe add faction?
    requiredLevel,
    minimumLevel,
    maximumLevel
    
}